[package]
name = "oxidar-lambdas"
version = "0.1.0"
edition = "2021"

[dependencies]
aws-config = "1.5.15"
aws-sdk-dynamodb = "1.62.0"
lambda_runtime = "0.13.0"
serde = "1"
tokio = { version = "1", features = ["macros"] }
